export ANDROID_HOME=$HOME/Library/Android/Sdk
export JAVA_HOME=$(/usr/libexec/java_home -v 1.8 2> /dev/null)
export ANDROID_JAVA_HOME=$(/usr/libexec/java_home -v 1.8 2> /dev/null)
export LC_ALL=en_US.UTF-8
export LC_CTYPE=en_US.UTF-8
export LANGUAGE=en_US.UTF-8
export LANG=en_US.UTF-8
export NNTPSERVER="news.epita.fr"
export EDITOR=vim
export MANPAGER="sh -c \"col -b | vim -c 'set ft=man ts=8 nomod nolist nonu colorcolumn=0' \
                      -c 'nnoremap i <nop>'                                                \
                      -c 'nnoremap a <nop>'                                                \
                      -c 'nnoremap <Space> <C-f>'                                          \
                      -c 'noremap q :quit<CR>' -\""

export DEV="$HOME/Developer/"
export KSRC="$DEV/linux"
export KBUILD_OUTPUT="$KSRC/build"
export TERM=xterm-256color
export DISPLAY=:0

export PATH=/usr/local/bin:$PATH
export PATH=/usr/local/sbin:$PATH
export PATH="/usr/local/opt/gnu-sed/libexec/gnubin:$PATH"
export PATH=$HOME/.cargo/bin:$PATH
export PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools
export PATH=$PATH:/Applications/Ada/bin/
export PATH="/usr/local/opt/bison/bin:$PATH"
#export PATH="/usr/local/opt/llvm/bin":$PATH

export LC_CTYPE=en_US.UTF-8
export LC_ALL=en_US.UTF-8

if [ $(uname) = Darwin ]; then
  alias ls='ls -G'
  alias l='ls -lah -G'
else
  alias ls='ls --color'
  alias l='ls -lah --color'
fi

alias cd="cd -P"

alias dev='cd $DEV'
alias code='cd $HOME/Documents/code'
alias proj='cd $HOME/Documents/code/projects'
alias term='exec urxvt -e zsh & clear'

alias ga='git add '
alias gs='git status'
alias gc='git commit -s'
alias gp='git push '
alias gd='git diff'
alias gl='git log --graph --abbrev-commit --decorate --date=relative --all '
alias go='git log --oneline --decorate --all '
alias glo='git log --oneline --decorate '
alias gop='git log --oneline --decorate --all --pretty="%C(yellow)%h %Cred%d %Creset%s %Cgreen%<(7)%aN%Creset"'
